{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "defaultLevel": {
            "title": "默认优先级",
            "description": "直接查询的响应优先级",
            "type": "number"
        },
        "hideUnknown": {
            "title": "隐藏未知类别",
            "description": "当查询到未知类别时, 发送提示而不回复图片",
            "type": "boolean"
        }
    }
}